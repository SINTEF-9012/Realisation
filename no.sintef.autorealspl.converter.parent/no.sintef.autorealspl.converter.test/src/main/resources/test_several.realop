operator SPpositivePos {
	pre : not realised(SPpositive);
	post : positive(SPpositive);
}

operator SPpositiveNeg {
	pre : not realised(SPpositive);
	post : negative(SPpositive);
}

operator SPnegativePos {
	pre : not realised(SPnegative);
	post : positive(SPnegative);
}

operator SPnegativeNeg {
	pre : not realised(SPnegative);
	post : negative(SPnegative);
}

operator SPnotresolvedPos {
	pre : not realised(SPnotresolved);
	post : positive(SPnotresolved);
}

operator SPnotresolvedNeg {
	pre : not realised(SPnotresolved);
	post : negative(SPnotresolved);
}